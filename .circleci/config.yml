version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12.6

    steps:
      - checkout

      #- run: go get -v -t -d ./...
      - run: cd api && GO111MODULE=on go test -v

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build